// import authStore from '@/stores/authStore';
// import React from 'react';

// interface AuthMainProviderProps {
//    children: React.ReactNode;
// }

// const AuthMainProvider: React.FC<AuthMainProviderProps> = ({ children }) => {
//    const { isLogin } = authStoreore();

//    if (isLogin) return children;

//    return <div>AuthMainProvider</div>;
// };

// export default AuthMainProvider;
